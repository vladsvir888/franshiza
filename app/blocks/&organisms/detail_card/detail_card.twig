<article class="detail-card">
  {% view "@title" with {
    title: {
      level: 2,
      class: "visually-hidden",
      text: "IT франшиза самого выгодного рекламного сервиса с гарантией окупаемости"
    }
  } %}
  {% view "@image" with {
    image: detail_card.image|merge({
      class: "detail-card__img",
      sizes: {
        width: 285,
        height: 207
      }
    })
  } %}
  <div class="detail-card__info">
    <dl class="detail-card__list">
      {% for item in detail_card.info.list.items %}
        <div class="detail-card__item {{ item.class }}">
          <dt class="detail-card__term">
            {{ item.term }}
          </dt>
          <dd class="detail-card__descr">
            {{ item.descr }}
          </dd>
        </div>
      {% endfor %}
    </dl>
    <div class="detail-card__links">
      {% view "@button" with {
        button: detail_card.info.site_btn|merge({
          class: "detail-card__info-btn",
          icon: {
            class: "detail-card__info-icon",
            name: "sprite-mono.svg#globe"
          }
        })
      } %}

      {% view "@button" with {
        button: detail_card.info.presentation_btn|merge({
          class: "detail-card__info-btn",
          icon: {
            class: "detail-card__info-icon",
            name: "sprite-mono.svg#file"
          }
        })
      } %}
    </div>
  </div>
  <div class="detail-card__specialist">
    <div class="detail-card__specialist-header">
      {% view "@image" with {
        image: detail_card.specialist.image|merge({
          class: "detail-card__img",
          sizes: {
            width: 82,
            height: 82
          }
        })
      } %}

      <div class="detail-card__specialist-content">
        <div class="detail-card__specialist-name">
          {{ detail_card.specialist.name }}
        </div>

        <p class="detail-card__specialist-job">
          {{ detail_card.specialist.job }}
        </p>
      </div>
    </div>

    <div class="detail-card__specialist-footer">
      {% view "@button" with {
        button: detail_card.specialist.add_to_list_btn|merge({
          class: "btn--btn btn--toggle-gray btn--toggle detail-card__specialist-btn",
          icon: {
            name: "sprite-multi.svg#square"
          }
        })
      } %}

      {% view "@button" with {
        button: detail_card.specialist.contact_btn|merge({
          class: "btn--btn btn--primary btn--large btn--scroll detail-card__specialist-btn"
        })
      } %}
    </div>
  </div>
</article>